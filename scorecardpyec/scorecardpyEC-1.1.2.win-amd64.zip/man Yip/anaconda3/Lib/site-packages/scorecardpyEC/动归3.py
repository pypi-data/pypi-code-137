import numpy as np
import time

data = {}
##28是个极大值了
max_num=29
for i in np.arange(1,max_num):
    if i<max_num-1:
        data[i]=np.arange(i+1,max_num)

c=[0]
def dfs(data, path, paths = []):   
    # c[0]=c[0]+1
    # print(c[0])
    datum = path[-1]           
    if datum in data:
        tmp = data[datum]
        # if len(tmp) > 10:
        #     tmp = tmp[0:10]
        for val in tmp:
            new_path = path + [val]
            dfs(data, new_path, paths)
            # paths = dfs(data, new_path, paths)
    # else:
    #     paths += [path]
    # return paths

t1 = time.time()
print(dfs(data = data, path = [1], paths = []))
t2 = time.time()
print(t2-t1)